// @modubiz/ui/components — shared UI component library
//
// Components are vendored here via shadcn/ui CLI.
// They use Radix primitives and are styled with Tailwind (logical properties only).
//
// Components will be added during Phase 2+ as the frontend shell is built.
// Example: Button, Input, Select, Dialog, DropdownMenu, Table, Badge, etc.
//
// Export pattern:
// ```typescript
// export { Button, buttonVariants } from './button';
// export type { ButtonProps } from './button';
// ```
