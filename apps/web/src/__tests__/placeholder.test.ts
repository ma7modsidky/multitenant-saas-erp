import { describe, expect, it } from 'vitest';

/**
 * Placeholder test for the web app.
 *
 * This ensures `vitest run` doesn't exit with code 1 when no real tests exist.
 * Remove this file once at least one real test is written.
 */
describe('web placeholder', () => {
  it('sanity check — the test runner works', () => {
    expect(1 + 1).toBe(2);
  });
});
