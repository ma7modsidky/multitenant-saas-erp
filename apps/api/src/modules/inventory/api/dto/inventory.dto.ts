import { z } from 'zod';

// Wire DTOs for the inventory module live here (see MODULE_GUIDE.md §4 Step 6).
// Example:
// export const createItemSchema = z
//   .object({ name: z.string().min(1).max(200) })
//   .strict();
// export type CreateItemDto = z.infer<typeof createItemSchema>;
