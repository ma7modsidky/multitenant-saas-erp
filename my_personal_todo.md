- platform
  - create teams within organization
  - each team has leader - members - each can be assigned to different roles
  - add emails services ( confirm account - reset password)

- crm
  - add dynamic pipelines stages with setup and templates
  - integrate teams into crm (deal ownership and manager assigns tasks and dea)

- inventory
  - unarchive product or variant not present
  - products list page show in table if product has variant, and add barcode
  - product detail page, when you add a variant it gets displayed by "Created by
    —·Last edited by —" not showing the real user who created or edited it, the
    add variant button in the variants section (not the one at the top of the
    page) opens the same add variant form at the top of the page which is not
    visible at the screen you have to scroll up to get to the form , the same
    problem with the edit variant button it opens the form at the top and thats
    very bad ui/ux
  - in the stock when I try to recieve a stock it just shows the last variant
    created of that product even if you have multiple, can we fix that

-translate modules.inventory.fields.code translate
modules.inventory.detail.cancel
